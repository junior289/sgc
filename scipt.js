document.querySelector('form').addEventListener('submit', function(event) {
    event.preventDefault();
    // Ajoute ici ton code pour gérer la soumission du formulaire
    alert('Formulaire soumis!');
  });
  